version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/currencies.db:/app/currencies.db

  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend